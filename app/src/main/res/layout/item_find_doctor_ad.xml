<?xml version="1.0" encoding="utf-8"?>
<!-- Native Ad Placeholder card for Find a Doctor screen -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cardAd"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="20dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="20dp"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@android:color/white"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Header Row: AD Label + Sponsor + More Options -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- AD Label -->
            <TextView
                android:id="@+id/tvAdLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_ad_label"
                android:fontFamily="@font/poppins_medium"
                android:text="AD"
                android:textColor="@color/text_hint"
                android:textSize="10sp" />

            <!-- Sponsor Text -->
            <TextView
                android:id="@+id/tvSponsor"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="10dp"
                android:layout_weight="1"
                android:fontFamily="@font/poppins"
                android:text="Sponsored by HealthPlus"
                android:textColor="@color/text_secondary"
                android:textSize="12sp" />

            <!-- More Options -->
            <ImageButton
                android:id="@+id/btnAdOptions"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Ad options"
                android:src="@drawable/ic_more_vert"
                app:tint="@color/text_hint" />

        </LinearLayout>

        <!-- Ad Content Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Ad Image Placeholder -->
            <FrameLayout
                android:layout_width="100dp"
                android:layout_height="100dp">

                <View
                    android:id="@+id/viewAdImagePlaceholder"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@drawable/bg_ad_image_placeholder" />

                <!-- AdMob MediaView will replace this -->
                <com.google.android.material.imageview.ShapeableImageView
                    android:id="@+id/ivAdImage"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:visibility="gone"
                    app:shapeAppearanceOverlay="@style/RoundedCorner12" />

            </FrameLayout>

            <!-- Ad Text Content -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="14dp"
                android:layout_weight="1"
                android:orientation="vertical">

                <!-- Ad Title / Headline -->
                <TextView
                    android:id="@+id/tvAdTitle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:fontFamily="@font/poppins_semibold"
                    android:text="Full Body Checkup"
                    android:textColor="@color/text_primary"
                    android:textSize="16sp" />

                <!-- Ad Description / Body -->
                <TextView
                    android:id="@+id/tvAdDescription"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:fontFamily="@font/poppins"
                    android:lineSpacingExtra="2dp"
                    android:maxLines="3"
                    android:text="Get 50% off your first comprehensive health screening. Limited time offer."
                    android:textColor="@color/text_secondary"
                    android:textSize="12sp" />

                <!-- CTA Button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnAdCta"
                    android:layout_width="wrap_content"
                    android:layout_height="36dp"
                    android:layout_marginTop="10dp"
                    android:backgroundTint="@color/docease_primary"
                    android:fontFamily="@font/poppins_semibold"
                    android:minWidth="100dp"
                    android:paddingHorizontal="20dp"
                    android:text="Book Now"
                    android:textColor="@android:color/white"
                    android:textSize="12sp"
                    app:cornerRadius="18dp"
                    app:elevation="0dp" />

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
